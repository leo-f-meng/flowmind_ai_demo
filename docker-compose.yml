services:
  db:
    image: postgres:16
    container_name: flowmind-db
    environment:
      POSTGRES_USER: flowmind
      POSTGRES_PASSWORD: flowmind
      POSTGRES_DB: flowmind
    ports:
      - "5432:5432"
    volumes:
      - flowmind_pg:/var/lib/postgresql/data

volumes:
  flowmind_pg: